{
  "compilerOptions": {
    "composite": true,
    "module": "esnext",
    "moduleResolution": "node",
    "baseUrl": ".",
    "paths": {
        "@/*": ["src/*"] // 相对位置需要配置baseUrl才能识别，否则会报错
    }
  },
}
